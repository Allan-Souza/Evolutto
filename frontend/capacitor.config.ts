import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.evolutto.app',
  appName: 'Evolutto',
  webDir: 'dist/frontend/browser'
};

export default config;
